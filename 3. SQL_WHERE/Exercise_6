SELECT city,
       country,
       accident_date,
       damage
FROM accidents
WHERE damage BETWEEN 100 AND 1000
AND country LIKE 'Germany';

--Original solution:
SELECT city,
       country,
       accident_date,
       damage
FROM accidents
WHERE damage > 100
AND   damage < 1000
AND   country = 'Germany';

